# 동행로컬 Claude Design 와이어프레임 요구사항

> **2026-09-21 구현 검토 반영:** 이 문서는 초기 와이어프레임 기록을 포함한다. 현재 점수·데이터 표시 계약은 [평가·근거 표시 계약](docs/evaluation-policy.md)이 우선한다. 아래 초기 문구의 ‘잠재력’은 화면에서 ‘관광권역 여건 점수’로 사용하고, ‘데이터 신뢰도’는 근거 없는 숫자 대신 검증 상태로 표시한다. 세부 지표값·가중치는 검증 전까지 만들지 않으며, 조사 완료만으로 조건 충족·신뢰도 상승을 자동 계산하지 않는다.

## Source of truth

- 상태: Draft
- 최종 갱신일: 2026-09-21
- 문서 목적: Claude Design에서 동행로컬 MVP의 와이어프레임과 화면 흐름을 제작하기 위한 단일 요구사항 문서
- 주요 제품 표면: 데스크톱 지도 중심 웹 대시보드, 태블릿 현장검증 화면, 정책 검토안 미리보기
- 검토한 근거:
  - `README.md`
  - Notion `동행로컬_서비스_기획_총정리`
  - Notion `codex 추천 구성`
  - 사용자의 명시적 피드백으로 확정한 `A. 지도 중심 관제형` (브라우저 클릭 기록보다 대화에서 밝힌 선택을 우선)
- 확정된 기술 방향: Next.js App Router + TypeScript + Supabase PostgreSQL/PostGIS + Vercel Functions/Cron
- 실증 지역: 경상북도 안동시
- MVP 관광권역: 원도심·월영교권, 하회마을권, 도산·예끼마을권

## Claude Design 작업 목표

아래 요구사항을 바탕으로 **낮은 충실도에서 중간 충실도 사이의 반응형 와이어프레임**을 제작한다.

단순 관광정보 사이트가 아니라, 안동시 관광·지역경제 담당자가 관광권역을 비교하고 사업 검토 우선순위를 결정하는 **공공 관광 의사결정 도구**로 보여야 한다.

선택한 화면 방향은 다음과 같다.

> 지도 중심 대시보드를 첫 화면이자 기본 업무 화면으로 사용하고, 안동의 세 관광권역과 선택 권역의 판단 근거를 한눈에 보여준다.

## Brand

- 성격: 신뢰할 수 있음, 근거 중심, 차분함, 지역 친화적, 실무적
- 핵심 인상: 공공기관 보고서의 신뢰성과 현대적인 데이터 제품의 명료함을 함께 제공
- 신뢰 신호:
  - 모든 점수에 데이터 기준일과 출처 표시
  - `확인된 데이터`, `추론`, `현장확인 필요` 상태를 시각적으로 구분
  - 잠재력 점수와 데이터 신뢰도를 분리
  - 추천 이유와 한계를 같은 화면에 표시
- 피해야 할 표현:
  - 여행 예약 서비스처럼 보이는 화려한 프로모션 UI
  - 근거 없는 AI 강조
  - 모든 수치를 확정 사실처럼 보이게 하는 표현
  - 전통문양을 과도하게 사용하는 관광 홍보물 스타일
  - 장식 목적의 3D 효과와 불필요한 애니메이션

## Product goals

### 목표

- 안동의 세 관광권역을 동일한 기준으로 비교한다.
- 관광수요가 체류·지역소비로 이어지지 못하는 병목을 찾는다.
- 관광약자 접근성과 데이터 부족을 숨기지 않고 별도로 표시한다.
- 점수에서 원천 데이터와 계산 근거까지 추적할 수 있게 한다.
- 분석 결과를 현장검증과 실행과제로 연결한다.
- 공모전 시연에서 `흩어진 데이터 → 비교 가능한 근거 → 실행 우선순위`의 변화를 3분 안에 보여준다.

### 비목표

- 관광객용 여행 일정 추천
- 예약·결제·실시간 객실 검색
- 전국 지자체 분석
- 실시간 교통·혼잡·대기시간 제공
- 생성형 AI가 점수나 사업효과를 임의 생성하는 기능
- 복잡한 다기관 권한관리

### 성공 신호

- 사용자가 세 권역의 차이와 우선 검토 이유를 1분 안에 설명할 수 있다.
- 추천 결과에서 데이터 출처와 기준일을 두 번 이하의 동작으로 확인할 수 있다.
- 낮은 신뢰도의 결과를 `즉시 투자 대상`이 아니라 `우선 현장검증 대상`으로 이해한다.
- 선택한 실행과제를 담당자·일정·KPI와 연결할 수 있다.

## Personas and jobs

### 주요 사용자

1. 안동시 관광 담당자
   - 다음 연도 관광사업 후보 권역을 비교한다.
   - 추천의 근거와 한계를 검토한다.
   - 현장조사와 실행과제를 등록한다.
2. 지역경제·상권 담당자
   - 방문수요가 지역 소비로 연결되는지 확인한다.
   - 시장·음식·숙박·지역사업자 연계 과제를 검토한다.
3. 의사결정권자
   - 제한된 예산을 어느 권역에 먼저 투입할지 판단한다.
   - 요약 결론과 신뢰도, 주요 위험을 빠르게 확인한다.

### 사용 맥락

- 주요 사용 환경: 1440px 이상 데스크톱, 회의실 노트북, 프로젝터
- 보조 사용 환경: 현장조사용 태블릿
- 모바일: 조회와 체크리스트 입력만 지원하며 전체 분석 편집은 제공하지 않는다.

## Information architecture

### 기본 내비게이션

- 상단 전역 바:
  - 동행로컬 로고/서비스명
  - 현재 지역 `안동시`
  - 분석 기준일
  - 데이터 갱신 상태
  - 사용자 메뉴
- 좌측 사이드바:
  - 관광권역 탐색
  - 후보 권역 비교
  - 근거 상세
  - 실행과제
  - 현장검증
- 전역 보조 기능:
  - 데이터 출처 보기
  - 용어 설명
  - 정책 검토안 내보내기

### 전체 사용자 흐름

```text
안동 관광권역 지도
→ 3개 권역 비교
→ 권역별 근거 상세
→ 병목과 우선 실행과제 확인
→ 현장검증 과제 등록
→ 정책 검토안 미리보기
```

## Required screens

### 화면 1. 안동 관광권역 탐색 지도

#### 목적

안동의 관광자원이 어디에 분포하고 세 권역이 어떻게 떨어져 있는지 한눈에 보여준다.

#### 필수 구성

- 중앙 60~65%: 안동 지도
- 좌측: 전역 내비게이션과 필터
- 우측: 선택한 권역 요약 패널
- 상단 요약 배너:
  - 헤드라인 `흩어진 관광 데이터를 실행 우선순위로`
  - 한 문장 설명 `안동의 관광권역을 비교하고, 체류·지역소비·접근성 병목을 근거와 함께 찾습니다.`
  - 현재 우선 검토 권역과 핵심 이유 1개
  - 보조 CTA `분석 방법 보기`
- 지도 표시:
  - 원도심·월영교권
  - 하회마을권
  - 도산·예끼마을권
  - 관광지, 숙박, 음식, 행사, 무장애 정보 마커
  - 권역 경계 또는 반경
- 필터:
  - 자원 유형
  - 데이터 확인 상태
  - 무장애 정보 유무
  - 기준 기간
- 우측 요약 패널:
  - 권역명과 한 줄 진단
  - 잠재력 점수
  - 데이터 신뢰도
  - 핵심 이유 3개
  - 데이터 기준일
  - CTA `상세 근거 보기`
- 하단 또는 지도 위 카드: 세 권역의 간단 비교

#### 중요 규칙

- 잠재력과 신뢰도는 서로 다른 색·레이블로 표시한다.
- 점수는 확률처럼 보이지 않게 `검토 점수`라는 용어를 사용한다.
- 낮은 신뢰도는 빨간색 실패가 아니라 `추가 조사 필요` 상태로 표시한다.
- 지도 범례는 항상 노출한다.

### 화면 2. 후보 권역 비교

#### 목적

세 권역의 차이를 동일한 지표로 비교하고 먼저 검토할 권역을 선정한다.

#### 필수 구성

- 상단 결론 카드:
  - `현재 우선 검토 권역`
  - 선택 이유
  - 데이터 신뢰도와 주요 한계
- 권역 비교 열 3개:
  - 원도심·월영교권
  - 하회마을권
  - 도산·예끼마을권
- 비교 지표:
  - 관광자원 기반 — 가중치 미확정
  - 체류·지역소비 지원 여건 — 가중치 미확정
  - 권역 내 이동 연결 — 가중치 미확정
  - 관광약자 이용 여건 — 가중치 미확정
  - 주민·환경 여건 — 가중치 미확정
- 각 권역에 별도로 표시할 정보:
  - 잠재력 점수
  - 데이터 신뢰도
  - 주요 강점
  - 가장 큰 병목
  - 미확인 데이터 수
- 차트:
  - 레이더 차트 또는 가로 막대 비교 중 하나만 사용
  - 정확한 값은 차트 옆 표로 함께 제공
- CTA:
  - `이 권역 상세 분석`
  - `현장검증 대상으로 지정`

#### 상태 표현

- `사업기획 우선 검토`
- `현장검증 우선`
- `중장기 관찰`

초기 와이어프레임의 모든 수치는 반드시 `예시 데이터`로 표시한다.

### 화면 3. 권역 근거 상세

#### 목적

사용자가 점수와 추천이 어떻게 만들어졌는지 역추적할 수 있게 한다.

#### 정보 우선순위

```text
추천 결론
→ 핵심 이유 3개
→ 잠재력 / 데이터 신뢰도
→ 다른 권역과의 차이
→ 지표별 기여도
→ 원천 데이터·기준일·공간 단위
→ 해석 한계
→ 현장 확인 항목
```

#### 필수 구성

- 권역 제목, 대표 이미지, 지도 미니뷰
- 결론과 권장 판단
- 점수 구성요소와 가중치
- 근거 카드 목록:
  - 지표명
  - 값과 단위
  - 기준 기간
  - 공간 범위
  - 출처 API
  - 데이터 상태
- `왜 이 점수인가?` 설명 패널
- 다른 권역과의 차이
- 해석 한계와 금지된 해석
- 원자료 상세를 여는 드로어 또는 모달
- CTA `실행과제 보기`

#### 데이터 상태 배지

- `확인된 데이터`: 공공 API에서 확보
- `추론`: 규칙 기반 계산 결과
- `현장확인 필요`: 운영·이동·접근성 확인 전
- `정보 없음`: 점수 0과 구분
- `예시`: 실제 수집 전 와이어프레임용 값

### 화면 4. 우선 실행과제

#### 목적

분석 결과를 담당자가 검토 가능한 정책·사업 과제로 변환한다.

#### 필수 구성

- 선택 권역과 핵심 병목
- 병목별 추천 과제 카드
- 각 과제 카드:
  - 과제명
  - 해결할 병목
  - 추천 근거
  - 예상 효과는 정성 표현만 사용
  - 확인할 KPI
  - 필요한 현장검증
  - 관련 부서
  - 우선순위
- CTA:
  - `현장검증 과제로 등록`
  - `사업 검토안에 포함`
  - `보류`
- 추천 근거를 화면 3으로 역추적하는 링크

#### 예시 과제

- 하회마을과 주변 음식·숙박 연결 검토
- 원도심 야간 콘텐츠와 전통시장 동선 강화
- 도산권 무차량 이동·관광약자 접근성 현장조사

예시 과제는 실제 데이터 분석 결과가 나오기 전에는 확정 추천으로 표시하지 않는다.

### 화면 5. 현장검증·실행관리

#### 목적

공공데이터로 확인할 수 없는 정보를 조사하고 추천 이후의 진행 상황을 관리한다.

#### 필수 구성

- 보드 또는 목록 보기 전환
- 상태:
  - 확인 전
  - 확인 중
  - 확인 완료
  - 개선 필요
- 과제 항목:
  - 권역
  - 조사 장소
  - 확인 질문
  - 담당자
  - 예정일
  - 사진·메모
  - 결과
  - 관련 실행과제
- 현장검증 체크리스트:
  - 실제 운영 여부
  - 운영시간·휴무일
  - 도보·대중교통 연결
  - 경사·턱·출입구·화장실
  - 주차·수용 능력
  - 지역상권 참여 가능성
- CTA `현장검증 내용 저장` — 조사 완료를 조건 충족이나 신뢰도 상승으로 환산하지 않는다.

#### 태블릿 대응

- 한 열 카드 목록으로 전환한다.
- 체크박스와 사진 첨부 영역은 터치하기 쉽게 만든다.
- 네트워크가 느릴 때 임시 저장 상태를 보여준다.

### 화면 6. 정책 검토안 미리보기

#### 목적

분석 결과를 의사결정권자에게 공유할 수 있는 짧은 검토안으로 정리한다.

#### 필수 구성

- 대상 지역과 분석 기준일
- 우선 검토 권역
- 핵심 근거 3개
- 잠재력과 데이터 신뢰도
- 가장 큰 데이터 한계
- 우선 실행과제
- 현장검증 상태
- KPI 초안
- 포함된 데이터 출처
- CTA:
  - `PDF 미리보기`
  - `링크 공유`

#### MVP 제약

- 생성형 AI 없이 정해진 템플릿으로 구성한다.
- 확인되지 않은 수치나 사업 효과를 생성하지 않는다.
- 내보내기 구현이 늦어질 경우 화면 미리보기까지만 MVP에 포함한다.

## Design principles

### 1. 결론보다 근거가 멀리 떨어지지 않게 한다

점수와 추천 옆에서 기준일·출처·한계를 확인할 수 있어야 한다.

### 2. 잠재력과 확실성을 혼동하지 않게 한다

잠재력 점수와 데이터 신뢰도를 항상 나란히 표시하되 시각적으로 분리한다.

### 3. 정보 부족을 실패로 표현하지 않는다

데이터가 없는 권역을 낮은 잠재력으로 처리하지 않고 `현장확인 필요`로 연결한다.

### 4. 지도는 장식이 아니라 판단 도구로 사용한다

지도 선택이 비교·상세·실행과제로 이어져야 한다. 의미 없는 관광지 마커 나열은 피한다.

### 5. 첫 화면에서 공간과 판단을 함께 보여준다

별도 소개 화면을 거치지 않고 지도, 세 권역, 선택 권역 요약을 동시에 보여준다. 서비스 설명은 지도 화면의 짧은 요약 배너와 분석 방법 패널로 제공한다.

## Visual language

### 색상

- 기본 배경: 따뜻한 아이보리 또는 아주 옅은 회색
- 주요 색상: 안동의 자연과 신뢰를 연상시키는 짙은 녹색
- 보조 색상: 남색, 흙빛 갈색, 차분한 청록
- 경고: 주황색
- 오류: 제한적인 적색
- 데이터 미확인: 중립 회색 또는 점선 표현
- 권역 색은 색각 이상에서도 구분되도록 색상과 패턴·레이블을 함께 사용한다.

### 타이포그래피

- 한국어 가독성이 높은 산세리프 계열
- 제목·점수·결론은 명확한 굵기 차이를 둔다.
- 최소 본문 크기 16px
- 긴 보고서 문장보다 짧은 판단 문구와 근거 목록을 우선한다.

### 레이아웃

- 8px 기반 간격 체계
- 카드 반경 8~12px
- 약한 테두리와 최소한의 그림자
- 한 화면에 핵심 CTA는 하나만 강하게 표시
- 표와 차트가 동시에 과밀해지지 않도록 상세 정보는 드로어로 분리

### 이미지와 아이콘

- 안동 관광지 이미지는 권역 식별과 맥락 제공에만 사용한다.
- 아이콘만으로 상태를 전달하지 않고 텍스트 레이블을 함께 사용한다.
- 전통문양은 구분선이나 작은 포인트 수준으로 제한한다.

### 모션

- 지도 권역 선택, 패널 전환, 선택 결과 강조에만 사용한다.
- 150~250ms 범위의 짧은 전환을 사용한다.
- 자동 재생, 패럴랙스, 큰 배경 영상은 사용하지 않는다.

## Components

### 공통 구성요소

- AppHeader
- SideNavigation
- AnalysisFreshnessBadge
- DataStatusBadge
- TourismRegionCard
- PotentialScore
- ConfidenceScore
- MetricContributionBar
- RegionComparisonTable
- MapLegend
- MapResourceMarker
- EvidenceCard
- EvidenceDrawer
- LimitationNotice
- BottleneckCard
- ActionTaskCard
- FieldValidationChecklist
- TaskStatusBoard
- ReportPreview

### 필수 상태

- 기본
- 선택됨
- 호버
- 키보드 포커스
- 로딩
- 데이터 없음
- 일부 데이터만 존재
- API 오류
- 오래된 데이터
- 현장확인 필요
- 저장 성공
- 저장 실패
- 비활성화

## Accessibility

- 목표 기준: WCAG 2.2 AA
- 키보드만으로 내비게이션·필터·카드·드로어를 조작할 수 있어야 한다.
- 모든 포커스 상태를 명확하게 표시한다.
- 차트와 지도에는 표 또는 텍스트 대안을 제공한다.
- 색상만으로 권역·상태·점수를 구분하지 않는다.
- 지도 마커에 접근 가능한 이름과 관광자원 유형을 제공한다.
- 모달·드로어가 열리면 포커스를 내부에 유지하고 닫은 뒤 원래 위치로 돌려보낸다.
- `prefers-reduced-motion`에서 불필요한 전환을 제거한다.

## Responsive behavior

### 데스크톱 1440px 이상

- 좌측 사이드바 + 중앙 콘텐츠 + 우측 상세 패널 구조
- 지도와 비교 정보를 동시에 표시
- 공모전 발표의 기본 해상도

### 노트북 1024~1439px

- 우측 상세 패널을 접을 수 있게 한다.
- 비교표는 가로 스크롤 대신 핵심 지표를 우선 노출한다.

### 태블릿 768~1023px

- 사이드바를 햄버거 또는 하단 내비게이션으로 전환한다.
- 지도와 상세 패널을 탭으로 분리한다.
- 현장검증 체크리스트 입력을 우선 지원한다.

### 모바일 767px 이하

- 권역 요약 조회와 현장검증 입력만 제공한다.
- 복잡한 비교표 편집과 지도 다중 패널은 제공하지 않는다.

## Interaction states

### Loading

- 지도와 카드 구조를 유지하는 스켈레톤을 사용한다.
- `관광 데이터를 불러오는 중`과 `분석 결과를 계산 중`을 구분한다.

### Empty

- 데이터가 없다는 사실과 아직 수집하지 않았다는 상태를 구분한다.
- 다음 행동 `데이터 수집 상태 확인`을 제공한다.

### Error

- 마지막 정상 분석 결과가 있으면 계속 보여준다.
- 오류 발생 시점과 영향을 받은 데이터만 표시한다.
- 내부 오류 코드 대신 사용자가 취할 행동을 안내한다.

### Stale data

- 오래된 자료를 숨기지 않고 기준일을 강조한다.
- `최신 데이터 수집 실패 — 마지막 정상 결과 표시 중` 상태를 제공한다.

### Success

- 실행과제 등록, 현장검증 저장, 보고서 생성 성공을 짧은 토스트와 화면 상태 변화로 알린다.

### Slow network

- 현장검증 입력은 저장 중·저장 완료·재시도 필요 상태를 명확히 표시한다.

## Content voice

- 어조: 단정적이기보다 근거 중심이고 실무적
- 권장 용어:
  - `검토 우선순위`
  - `잠재력 점수`
  - `데이터 신뢰도`
  - `현장확인 필요`
  - `기준 기간`
  - `데이터 한계`
- 피할 용어:
  - `성공 확률`
  - `최적 투자처`
  - `AI가 선정한 정답`
  - `접근 불가능` — 데이터가 없을 때 사용 금지
- 숫자는 단위와 기준 기간을 함께 표시한다.
- 예시 수치는 `예시 데이터` 배지를 반드시 붙인다.

## Data and reasoning requirements

### 분석 점수

- 후보 영역: 관광자원 기반, 체류·지역소비 지원 여건, 권역 내 이동 연결, 관광약자 이용 여건, 주민·환경 여건.
- 영역·지표 가중치는 미확정이며 문헌·지역 검토·전문가 조사 후 정한다.
- 데모 총점은 명시적인 시연값이고 실제 지표 구성이나 라이브 산출 근거로 사용하지 않는다.
- 지표 입력이 없으면 비교·상세에서 모두 `지표 근거 미등록`/`산출 대기`를 표시한다.

기존 30·25·20·15·10%는 검증되지 않은 초기 가설이므로 현재 화면·계산의 기준으로 쓰지 않는다.

### 데이터 신뢰도

아래 요소는 개별 상태로 확인한다. 검증된 산식 없이 숫자나 확률로 합치지 않는다. 자료 확보율과 통계적 신뢰도도 서로 구분한다.

- 최신성
- 완전성
- 직접성
- 공간 적합성
- 출처 다양성
- 현장 검증 여부

### 데이터 표현 원칙

- `0`과 `정보 없음`을 구분한다.
- 관측값·예측값·추정값을 구분한다.
- 차량 내비게이션 데이터를 전체 관광객 이동으로 표현하지 않는다.
- 시·군 단위 값을 작은 관광권역의 실측값처럼 표현하지 않는다.
- 추천에서 적용 규칙, 지표, 기준일, 출처로 역추적할 수 있어야 한다.

## Implementation constraints

- 프레임워크: Next.js App Router
- 언어: TypeScript
- 배포: Vercel
- 데이터베이스: Supabase PostgreSQL
- 공간 기능: PostGIS
- 지도: Kakao Maps JavaScript API 우선 검토
- 수집: Vercel Cron이 한국관광공사 API를 작은 단위로 호출
- 화면은 마지막 정상 발행 분석 버전만 조회
- 브라우저에 공공데이터 API 서비스 키, DB 비밀번호, service-role 키를 노출하지 않는다.
- 서버리스 연결에 적합한 DB 풀링을 사용한다.
- 와이어프레임은 실제 프레임워크 컴포넌트 구현보다 정보구조와 상호작용 검증을 우선한다.

## Claude Design 출력 요구사항

1. 데스크톱 1440px 기준으로 화면 1~6을 모두 제작한다.
2. 화면 1은 사용자가 선택한 `A. 지도 중심 관제형`으로 제작하며 제품의 첫 화면이자 기본 업무 화면으로 사용한다.
3. 별도 스토리형 진입 화면은 만들지 않는다. 서비스 설명이 필요하면 화면 1의 요약 배너 또는 분석 방법 패널로 제공한다.
4. 화면 간 연결 관계를 프로토타입 링크 또는 화살표로 표현한다.
5. 실제 한국어 문구를 사용하고 Lorem Ipsum은 사용하지 않는다.
6. 수치는 모두 `예시 데이터`로 표시한다.
7. `잠재력`, `데이터 신뢰도`, `현장확인 필요`가 시각적으로 혼동되지 않아야 한다.
8. 지도 없이도 이해할 수 있는 텍스트·표 대안을 포함한다.
9. 정상 화면뿐 아니라 로딩, 데이터 없음, 일부 데이터, API 오류 상태를 최소 한 번씩 보여준다.
10. 재사용 가능한 컴포넌트 이름을 프레임 또는 그룹 이름에 사용한다.
11. 발표 시연에서도 화면 1의 지도에서 시작해 비교·근거·실행과제로 자연스럽게 이동해야 한다.
12. 관광객용 여행 추천·예약 서비스처럼 보이지 않아야 한다.

## Wireframe acceptance checklist

- [ ] 안동의 세 관광권역이 모든 핵심 화면에서 동일한 명칭으로 사용된다.
- [ ] 첫 화면에서 지도, 안동의 세 관광권역, 선택 권역 요약을 동시에 확인할 수 있다.
- [ ] 세 권역의 잠재력과 데이터 신뢰도를 동시에 비교할 수 있다.
- [ ] 점수의 근거와 데이터 한계를 두 번 이하의 동작으로 확인할 수 있다.
- [ ] 데이터 없음과 실제 0이 다르게 표시된다.
- [ ] 낮은 신뢰도의 결과가 현장검증 과제로 연결된다.
- [ ] 추천 과제에서 원천 근거 화면으로 돌아갈 수 있다.
- [ ] 현장검증 결과를 저장하고 상태 변화를 확인할 수 있다.
- [ ] 정책 검토안에 기준일·출처·한계가 포함된다.
- [ ] 데스크톱과 태블릿의 핵심 레이아웃 변화가 정의돼 있다.
- [ ] 지도·차트에 접근 가능한 대체 표현이 있다.
- [ ] 모든 예시 수치에 `예시 데이터` 표시가 있다.

## Open questions

- [ ] 동행로컬의 최종 로고와 브랜드 자산 — 현재 없음, 와이어프레임에서는 텍스트 로고 사용
- [ ] Kakao Maps API 사용 확정 여부 — 키 발급과 도메인 등록 전 검증 필요
- [ ] 한국관광공사 API별 안동 데이터 반환 건수와 결측률 — 실제 서비스 키 응답으로 검증 필요
- [ ] 정책 검토안 내보내기 형식 — PDF 우선 가정
- [ ] 실제 현장검증 담당자와 상태 변경 권한 — MVP는 단일 조직 가정
- [ ] 점수 가중치 조절 UI — 1차 와이어프레임에서는 읽기 전용으로 가정
